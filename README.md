# VIJETA WINGS — GitHub + Render

This package uses the supplied `Wings.html` as the website base.

## Structure
- `public/index.html` — Wings.html (renamed for web hosting)
- `server.js` — lightweight Node server for Render
- `package.json` — Render start configuration

## Render
Runtime: Node
Build Command: leave blank
Start Command: `npm start`
